# APP INFO:
This is the very first React CRUD Application by me.

``Date: Januray 1, 2026``

```markdown
Notes:
- main.jsx e bootstrap, tailwindCss, etc CSS import korte hoy
- Go to `index.html <head />` section to change App's Tab name and Icon.
  
- Public Folder e joidi kono Assets rakho then you can link them with root/filename.ext
  That means accessible from React App Root (Imagine!).

```

```markdown
# Sequence of Depth REACT-JS
1. index.html -> has id#root
2.     Src/
3.         main.jsx -> puts App.jsx into that #root
4.             App.jsx -> Should have all the routes
            
```