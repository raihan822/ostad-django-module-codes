import "./LoaderComponent.css"

export default function LoaderComponent(){
    // used a ready to use Loading-Animation-CSS in here.
    return (
        <div className="container loader-wrapper">
            <div className="loader"></div>
        </div>
    );
}