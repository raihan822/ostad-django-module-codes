# v1: Fast API Intro
- FastApi is a web-framework.
- automatic data validation with pydentic, supports async programming.

# v2: 
cd to that folder,
```
python -m venv venv
source venv/bin/activate

pip install fastapi uvicorn pydentic
```
* RUN uvicorn server:
`uvicorn py_file:fastapi_app_obj --reload`
reload mane changes korle direct result dekhabe.
